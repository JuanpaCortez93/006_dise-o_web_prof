function efecto() {
    // $('.caja').toggle();
    // $('.caja').fadeIn(3000);
    // $('.caja').fadeToggle(3000);
    // $('.caja').slideDown();
    // $('.caja').slideUp();
    $('.caja').slideToggle();
}

$(document).ready(() => {
    // $(selector).efecto(velocidad, callback);

    // $('.caja').hide(3000, () => {
    //     alert("finished")
    // });

    // $('.caja').show(4000, () => {
    //     alert("show")
    // });

    

});